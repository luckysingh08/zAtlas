import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { isSupabaseConfigured, supabase } from '../supabaseClient'
import { useAuth } from '../contexts/AuthContext'
import { Role } from '../types'
import { Ambient } from '../components/AppShell'
export default function Login(){const{session,profile,refreshProfile}=useAuth();const navigate=useNavigate();const[mode,setMode]=useState<'signin'|'signup'>('signin'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false),[groupMode,setGroupMode]=useState<'create'|'join'>('create'),[name,setName]=useState(''),[role,setRole]=useState<Role>('student'),[group,setGroup]=useState(''),[code,setCode]=useState('');async function auth(e:React.FormEvent){e.preventDefault();setError('');setLoading(true);const{error}=mode==='signup'?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);setLoading(false)}async function complete(e:React.FormEvent){e.preventDefault();if(!session?.user)return;setError('');setLoading(true);if(groupMode==='create'){const{error}=await supabase.rpc('create_family_group',{p_name:name,p_role:role,p_group_name:group});if(error)setError(error.message)}else{const{error:profileError}=await supabase.from('profiles').insert({id:session.user.id,group_id:null,name,role});if(profileError)setError(profileError.message);else{const{error}=await supabase.rpc('join_group_by_code',{code:code.trim().toUpperCase()});if(error)setError(error.message)}}await refreshProfile();setLoading(false);navigate('/dashboard')}if(session&&profile?.group_id)return null;const joining=!!session;return <main className="auth"><Ambient variant={joining?'subject':'atlas'}/><section className="surface auth-card">{!joining?<><button className="brand"><span className="brand-mark">z</span><span>Atlas</span></button><h1>Learning, in orbit.</h1><p>A quieter home for your study rhythm and the people cheering you on.</p>{!isSupabaseConfigured&&<p className="error">Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to a local .env file to enable sign in.</p>}<div className="auth-tabs"><button className={mode==='signin'?'active':''} onClick={()=>setMode('signin')}>Sign in</button><button className={mode==='signup'?'active':''} onClick={()=>setMode('signup')}>Create account</button></div><form onSubmit={auth} className="form-stack"><input className="field" type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6}/>{error&&<p className="error">{error}</p>}<button className="btn btn-primary" style={{width:'100%',marginTop:14}} disabled={loading}>{loading?'Just a moment…':mode==='signin'?'Continue to Atlas':'Create your space'}</button></form><button className="btn" style={{width:'100%',marginTop:10}} onClick={async()=>{const{error}=await supabase.auth.signInWithOAuth({provider:'google'});if(error)setError(error.message)}}>Continue with Google</button></>:<><p className="eyebrow">One last detail</p><h2>Make your shared space feel personal.</h2><div className="auth-tabs"><button className={groupMode==='create'?'active':''} onClick={()=>setGroupMode('create')}>Create group</button><button className={groupMode==='join'?'active':''} onClick={()=>setGroupMode('join')}>Join group</button></div><form onSubmit={complete} className="form-stack"><input className="field" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)} required/><div className="auth-tabs"><button type="button" className={role==='student'?'active':''} onClick={()=>setRole('student')}>Student</button><button type="button" className={role==='guardian'?'active':''} onClick={()=>setRole('guardian')}>Guardian</button></div>{groupMode==='create'?<input className="field" placeholder="Family group name" value={group} onChange={e=>setGroup(e.target.value)} required/>:<input className="field" placeholder="Invite code" value={code} onChange={e=>setCode(e.target.value)} required/>}{error&&<p className="error">{error}</p>}<button className="btn btn-primary" style={{width:'100%',marginTop:14}} disabled={loading}>{loading?'Preparing your Atlas…':groupMode==='create'?'Create group':'Join group'}</button></form></>}</section></main>}
